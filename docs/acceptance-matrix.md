# 需求验收矩阵

验收日期：2026-08-18。原始需求：`E:\InternationalPublicHealth\001-需求.docx`。

| 需求模块 | 实现与证据 | 状态 |
|---|---|---|
| 双平台 | 独立 ASGI、数据库、Redis、JWT audience、前端入口/产物/镜像；路由和 bundle 隔离测试 | 通过 |
| 六源采集 | 6 个适配器、配置周期、调度热更新、重试、原始留存、统一 schema、指纹去重 | 通过 |
| 用户信息源与周期 | 预设仅首次导入；用户可增改停删；固定间隔/Cron/IANA 时区、解析模式、提示词和模型均入库并热更新；单源失败隔离 | 通过 |
| 双侧大语言模型 | 两侧独立供应商/密钥/默认模型；五类协议；模型发现、真实连接测试、事件研判和采集提取；引用删除保护 | 通过 |
| 数据/API | PostgreSQL/SQLite、Alembic 4 级迁移、分页过滤排序、Redis/本地降级缓存、OpenAPI、地图 4 接口 | 通过 |
| 风险评分 | 六因子、四级边界、中转、人员往来、趋势、预警、历史快照、全量重算 | 通过 |
| 互联网前端 | 仪表盘、事件、风险、采集、规则、摆渡；所有运行指标来自 API | 通过 |
| 地图 | 92vh 展开/收叠、6 图层、点击/悬停、下钻、真实 14 日事件与风险时间轴、PNG 导出、本地数据 | 通过 |
| 摆渡 | 文件/RabbitMQ/API 三通道、全量/增量、分片、加密、签名、校验、重试、幂等版本入库 | 通过 |
| 内网镜像 | 镜像态势、口岸图层、接收监控、预警与布控建议 | 通过 |
| 规则引擎 | `all/any/not`、比较器、5 类动作、版本、发布、在线测试、执行日志、热更新 | 通过 |
| 口岸对接 | REST 单/批、CSV/JSONL 上传、多目录自动扫描、14 日旅居、中转/申报、加密与脱敏 | 通过 |
| 身份权限 | 五角色、bcrypt、复杂度、锁定、JWT、30 分钟空闲、TOTP、LDAP/AD、本地账号保留 | 通过 |
| 系统管理 | 真实统计与时间窗/导出、审计筛选/导出、完整备份、WAL、恢复、用户管理、模型供应商管理 | 通过 |
| 内网离线 | 无 CDN、本地地图、CSP、静态外联扫描、socket 断网测试、镜像离线导入脚本 | 通过 |
| 文档 | README、架构、部署、运维、用户、接口、安全、验收矩阵 | 通过 |

## 自动化结果

- 后端：52 项测试通过；语句覆盖率 82.24%（要求 >70%）。
- 性能：自动验收通过——全量评分 <10 秒、单旅客匹配 <100 ms、API <2 秒。
- 前端：TypeScript 通过；Vitest 6 项通过；互联网/内网生产构建成功；bundle 路由隔离通过；内网产物外联扫描通过。
- 数据摆渡：AES-256-GCM/Ed25519 与 SM4-CBC/SM2-SM3、分片重组、API outbox、幂等均有自动测试。
- Compose：`docker compose config --quiet` 通过。

## 本机环境限制

本机 Docker 客户端可用，但 `dockerDesktopLinuxEngine` 管道不存在；Docker Desktop Linux 引擎未运行，且本机没有 Podman/Buildah，因此无法在此次验收中完成镜像实际构建/启动。代码、Dockerfile 和 Compose 已完成静态验证；在可用 Docker Engine 主机上执行 `docker compose build && docker compose up -d` 是上线前最后一项环境验证，不属于应用代码缺陷。
