FROM node:22-alpine AS build
WORKDIR /src
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run typecheck && npm run build:internet
FROM nginxinc/nginx-unprivileged:1.28-alpine
COPY deploy/nginx-internet.conf /etc/nginx/conf.d/default.conf
COPY --from=build /src/dist/internet /usr/share/nginx/html
EXPOSE 8080
